using UnityEngine;

// Enum to differentiate different colored objects from each other.
public enum ObjectColor {white, blue, purple}

public class ColorBehaviour : MonoBehaviour
{
    public ObjectColor objectColor;
}